# Units

eggcalc supports unit conversions across multiple categories.

## Length

| Unit | Name | Aliases |
|------|------|---------|
| `m` | meters | meter, meters |
| `km` | kilometers | kilometer, kilometers |
| `cm` | centimeters | centimeter, centimeters |
| `mm` | millimeters | millimeter, millimeters |
| `μm` | micrometers | micrometer, micrometers, um |
| `nm` | nanometers | nanometer, nanometers |
| `pm` | picometers | picometer, picometers |
| `in` | inches | inch, inches |
| `ft` | feet | foot, feet |
| `yd` | yards | yard, yards |
| `mi` | miles | mile, miles |
| `ly` | lightyears | lightyear, lightyears |
| `au` | astronomical units | au |
| `pc` | parsecs | parsec, parsecs |
| `angstrom` | angstroms | angstrom, angstroms |
| `fermi` | fermis | fermi |
| `nmi` | nautical miles | nauticalmile, nauticalmiles |
| `furlong` | furlongs | furlong, furlongs |
| `chain` | chains | chain, chains |
| `rd` | rods | rod, rods |
| `fathom` | fathoms | fathom, fathoms |
| `smoot` | smoots | smoot, smoots |

## Time

| Unit | Name | Aliases |
|------|------|---------|
| `s` | seconds | second, seconds |
| `ms` | milliseconds | millisecond, milliseconds |
| `μs` | microseconds | microsecond, microseconds, us |
| `ns` | nanoseconds | nanosecond, nanoseconds |
| `ps` | picoseconds | picosecond, picoseconds |
| `min` | minutes | minute, minutes |
| `h` | hours | hour, hours, hr |
| `d` | days | day, days |
| `wk` | weeks | week, weeks |
| `yr` | years | year, years |
| `fortnight` | fortnights | fortnights |
| `decade` | decades | decades |
| `century` | centuries | centuries |
| `millennium` | millennia | millennia |

## Data

| Unit | Name | Aliases |
|------|------|---------|
| `bit` | bits | bits |
| `B` | bytes | byte, bytes |
| `KB` | kilobytes | kilobyte, kilobytes |
| `MB` | megabytes | megabyte, megabytes |
| `GB` | gigabytes | gigabyte, gigabytes |
| `TB` | terabytes | terabyte, terabytes |
| `PB` | petabytes | petabyte, petabytes |
| `EB` | exabytes | exabyte, exabytes |
| `ZB` | zettabytes | zettabyte, zettabytes |
| `YB` | yottabytes | yottabyte, yottabytes |

## Mass

| Unit | Name | Aliases |
|------|------|---------|
| `kg` | kilograms | kilogram, kilograms |
| `g` | grams | gram, grams |
| `mg` | milligrams | milligram, milligrams |
| `μg` | micrograms | microgram, micrograms, ug |
| `ng` | nanograms | nanogram, nanograms |
| `lb` | pounds | pound, pounds, lbs |
| `oz` | ounces | ounce, ounces |
| `ton` | tons | tons |
| `tonne` | tonnes | tonnes |
| `stone` | stones | stones |
| `slug` | slugs | slugs |
| `ct` | carats | carat, carats |
| `gr` | grains | grain, grains |
| `dr` | drams | dram, drams |

## Volume

| Unit | Name | Aliases |
|------|------|---------|
| `L` | liters | liter, liters |
| `l` | liters | l (lowercase alias) |
| `mL` | milliliters | milliliter, milliliters |
| `μL` | microliters | microliter, microliters, uL |
| `gal` | gallons | gallon, gallons |
| `qt` | quarts | quart, quarts |
| `pt` | pints | pint, pints |
| `cup` | cups | cup, cups |
| `floz` | fluid ounces | fl oz, fluidounce, fluidounces |
| `tbsp` | tablespoons | tablespoon, tablespoons |
| `tsp` | teaspoons | teaspoon, teaspoons |

## Temperature

| Unit | Name |
|------|------|
| `C` | Celsius |
| `F` | Fahrenheit |
| `K` | Kelvin |
| `Ra` | Rankine |

## Pressure

| Unit | Name |
|------|------|
| `Pa` | Pascals |
| `kPa` | kilopascals |
| `MPa` | megapascals |
| `GPa` | gigapascals |
| `bar` | bar |
| `mbar` | millibars |
| `atm` | atmospheres |
| `psi` | pounds per square inch |

## Energy

| Unit | Name |
|------|------|
| `J` | Joules |
| `kJ` | kilojoules |
| `MJ` | megajoules |
| `GJ` | gigajoules |
| `cal` | calories |
| `kcal` | kilocalories |
| `Wh` | watt-hours |
| `kWh` | kilowatt-hours |
| `BTU` | British thermal units |
| `eV` | electronvolts |

## Power

| Unit | Name |
|------|------|
| `W` | Watts |
| `kW` | kilowatts |
| `MW` | megawatts |
| `GW` | gigawatts |
| `mW` | milliwatts |
| `hp` | horsepower |

## Data Transfer Rate

| Unit | Name |
|------|------|
| `bps` | bits per second |
| `Kbps` | kilobits per second |
| `Mbps` | megabits per second |
| `Gbps` | gigabits per second |

## Force

| Unit | Name |
|------|------|
| `N` | Newtons |
| `kN` | kilonewtons |
| `mN` | millinewtons |
| `dyne` | dynes |
| `lbf` | pound-force |

## Voltage

| Unit | Name |
|------|------|
| `V` | Volts |
| `kV` | kilovolts |
| `mV` | millivolts |
| `μV` | microvolts |

## Current

| Unit | Name |
|------|------|
| `A` | Amperes |
| `mA` | milliamperes |
| `μA` | microamperes |

## Angles

| Unit | Name |
|------|------|
| `rad` | radians |
| `deg` | degrees |

## Speed

| Unit | Name | Aliases |
|------|------|---------|
| `m/s` | meters per second | mps |
| `km/h` | kilometers per hour | kph |
| `mph` | miles per hour | mileperhour, milesperhour |
| `kn` | knots | knot, knots, kt |
| `mach` | mach | mach |

## Area

| Unit | Name | Aliases |
|------|------|---------|
| `m2` | square meters | m^2, sqm, squaremeter |
| `km2` | square kilometers | km^2, squarekilometer |
| `cm2` | square centimeters | cm^2, squarecentimeter |
| `mm2` | square millimeters | mm^2, squaremillimeter |
| `ha` | hectares | hectare, hectares |
| `acre` | acres | acres |
| `ft2` | square feet | ft^2, sqft, squarefoot |
| `in2` | square inches | in^2, sqin, squareinch |
| `mi2` | square miles | mi^2, sqmi, squaremile |
| `yd2` | square yards | yd^2, sqyd, squareyard |

## Frequency

| Unit | Name |
|------|------|
| `Hz` | hertz |
| `kHz` | kilohertz |
| `MHz` | megahertz |
| `GHz` | gigahertz |
| `THz` | terahertz |

## Usage

### Conversion Patterns

**"X in Y" pattern** - Convert a value to a different unit:

```bash
calc "100ft in meters"    # 30.48 m
calc "5km to miles"        # 3.107 mi
calc "1GB in MB"           # 1024 MB
calc "100C in F"          # 212 F
```

**Direct arithmetic** - Units are combined automatically:

```bash
calc "30m + 100ft"         # 60.48 m (feet converted to meters)
calc "1GB - 512MB"         # 512 MB
calc "2h + 30min"          # 2.5 h
calc "100km / 2h"          # 50 km/h
calc "60mi / h"            # 60 mi/h
```

**Understanding unit handling:**

```bash
# Numbers directly followed by units are treated as quantities
calc "30m"                 # 30 m (30 meters)
calc "5km"                 # 5 km (5 kilometers)

# During arithmetic, incompatible units are converted to match
calc "30m + 100ft"         # 60.48 m
```

### Unit Categories

Units must be from the same category for addition/subtraction:

| Category | Units |
|----------|-------|
| Length | m, km, cm, mm, ft, in, mi, etc. |
| Time | s, ms, min, h, d, wk, yr |
| Mass | kg, g, mg, lb, oz, ton |
| Volume | L, mL, gal, qt, pt, cup |
| Data | B, KB, MB, GB, TB, PB |
| Speed | m/s, km/h, mph, kn |
| etc. |

**Incompatible units cannot be added:**

```bash
calc "30m + 5kg"          # Error - length + mass incompatible
calc "30m + 5"            # Error - dimensional + dimensionless incompatible
```

### Temperature Conversions

Use the `temp()` function with three arguments: `temp(value, from_unit, to_unit)`

```bash
calc "temp(100, C, F)"
# 212 F

calc "temp(32, F, C)"
# 0 C

calc "temp(0, C, K)"
# 273.15 K

calc "temp(212, F, C)"
# 100 C
```

**Common conversions:**

| From | To | Formula |
|------|----|---------|
| Celsius | Fahrenheit | `temp(x, C, F)` → x * 1.8 + 32 |
| Fahrenheit | Celsius | `temp(x, F, C)` → (x - 32) / 1.8 |
| Celsius | Kelvin | `temp(x, C, K)` → x + 273.15 |

## UnitValue Methods

The `UnitValue` class represents a numeric value with optional units:

```python
from eggcalc import UnitValue

uv = UnitValue(30, "m")
```

**Constructor:**
- `UnitValue(value: float, unit: str | None = None)`

**String representation:**
- `__repr__()` → `"30 m"` (value and unit)
- `__str__()` → `"30 m"` (delegates to `__repr__`)
- `__format__(format_spec)` → `"30.00 m"` (formats value with spec)

**Comparison:**
- `__eq__(other)` → `True`/`False` (compares value and unit, uses epsilon for floats)
- Unitless values compare equal to their numeric equivalents

**Hashing:**
- `__hash__()` → Hash of `(value, unit)` tuple

**Arithmetic operations:**
- `__add__`, `__sub__`, `__mul__`, `__truediv__`, `__pow__`, `__neg__`, `__abs__`, `__round__`
- Automatic unit conversion when adding/subtracting compatible units

**Type conversions:**
- `__int__()` → `int(value)`
- `__float__()` → `float(value)`
- `__complex__()` → `complex(value)`

**Unit conversion:**
- `convert_to(target_unit)` → Returns new `UnitValue` in target unit

## Python API

```python
from eggcalc import evaluate_raw, UnitValue, get_conversion_factor

# Evaluate with units
result = evaluate_raw("30m + 100ft")
print(result)  # 60.48 m

# Work with UnitValue results
result = evaluate_raw("30m + 100ft")
if isinstance(result, UnitValue):
    print(f"Value: {result.value}, Unit: {result.unit}")

# Get conversion factor between any two units
factor = get_conversion_factor("ft", "m")
print(f"1 foot = {factor} meters")  # 0.3048

factor = get_conversion_factor("km", "mi")
print(f"1 km = {factor} miles")  # 0.621371

# Check if text is a recognized unit
from eggcalc import is_unit
print(is_unit("m"))     # True
print(is_unit("kg"))    # True
print(is_unit("xyz"))   # False

# List all supported units (returns alias strings)
from eggcalc import get_all_units
units = get_all_units()
print(f"Total unit aliases: {len(units)}")  # 508 aliases covering 150 canonical units

# Temperature conversion requires temp(value, from_unit, to_unit)
result = evaluate_raw("temp(100, C, F)")
print(result)  # 212.0
```

## Unit Utility Functions

```python
from eggcalc import normalize_unit, get_unit_category

# Normalize unit to canonical form
normalize_unit("meters")     # "m"
normalize_unit("kilometers")  # "km"
normalize_unit("feet")        # "ft"

# Get unit category
get_unit_category("m")    # "length"
get_unit_category("kg")   # "mass"
get_unit_category("gal")  # "volume"
```
