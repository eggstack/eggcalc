[tool.poetry]
name = "poetry-demo"
version = "0.1.0"
description = "A demo poetry project"
authors = ["Dev <dev@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.31"

[tool.poetry.dev-dependencies]
pytest = "^7.0"
ruff = "^0.1.0"

[tool.poetry.scripts]
demo = "demo:main"
