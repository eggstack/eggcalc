[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"

[project]
name = "pdm-demo"
version = "0.3.0"
requires-python = ">=3.10"

[tool.pdm.dev-dependencies]
lint = ["ruff", "mypy"]
test = ["pytest"]
