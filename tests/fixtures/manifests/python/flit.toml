[build-system]
requires = ["flit_core>=3.4,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "flit-demo"
version = "0.5.0"
description = "A flit demo project"

[tool.flit.module]
name = "flit_demo"
